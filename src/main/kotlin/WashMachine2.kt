fun main () {
    val washItemList = readln().split(" ").map { it -> it.toInt() }

    val numberMachines = readln().toInt()

    val washingList = IntArray(numberMachines)

    // find free machine
    washingList

}